name: ufo_login_app
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flow_builder: ^0.0.10
  flutter_bloc: ^8.1.3
  http: ^1.1.2
  equatable: ^2.0.5
  get_it: ^7.6.6


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/ufo_logo.png

